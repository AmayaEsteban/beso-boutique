// NO "use client"
import type { ReactNode } from "react";

export const metadata = {
  title: "Cancelar suscripción | BESO Boutique",
  description: "Deja de recibir nuestro newsletter cuando quieras.",
};

export default function UnsubscribeLayout({
  children,
}: {
  children: ReactNode;
}) {
  return children;
}
